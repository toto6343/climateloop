"""외부 공공데이터 API 연동 계층.

이 패키지 밖으로 나가는 값은 전부 "성공했으면 값, 실패했으면 None" 두 가지뿐이다.
호출부(main.py)는 None 을 받으면 기존 로컬 추정 계산으로 떨어진다 — 예외를 밖으로
흘리지 않는 이유는, 외부 API 하나가 죽었다고 시뮬레이터 화면이 깨질 이유가 없기
때문이다. openrouter_client 가 AI 실패를 폴백 문장으로 덮는 것과 같은 규칙이다.

값의 출처는 절대 섞이지 않는다. 실데이터를 쓴 응답만 data_source="live" 로 표기하고,
한 항목이라도 추정값으로 떨어지면 그 항목은 "fallback" 이다 (README 9.3 면책 원칙).
확신이 없으면 항상 "fallback" 쪽으로 둔다.
"""
