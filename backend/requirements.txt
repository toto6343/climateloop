# 버전 고정 기준: 2026-08-03 동작 확인 환경
#
# 미고정 상태에서 두 차례 장애가 있었다.
#   1) gemini-1.5-flash 가 은퇴해 404
#   2) langchain-core 1.x 부터 response.content 가 str -> list 로 변경되어
#      ai_message 가 객체 배열로 직렬화됨
# 2번은 지금도 유효하다 — 그래프의 call_llm 노드가 .text 로 받는 이유가 이것이다.
# 따라서 실제 장애를 일으킨 계열은 정확히 고정한다.
#
# Gemini 를 직접 부르던 계열(langchain-google-genai, google-generativeai)은 뺐다.
# LLM 호출은 ChatOpenAI 를 OpenRouter base_url 로 돌려 쓰므로 더 이상 필요 없다.

# Web Framework
fastapi==0.136.1

# ASGI Server
uvicorn[standard]==0.38.0

# AI / LangGraph
# 흐름 관리(계산 결과 -> 컨텍스트 구성 -> LLM 호출 -> 후처리)는 LangGraph 가,
# 실제 호출은 langchain-openai 의 ChatOpenAI 가 맡는다. OpenRouter 는 OpenAI
# 호환 API 라 base_url 만 갈아끼우면 그대로 동작한다.
langgraph==1.2.4
langchain-openai==1.3.2
# 직접 임포트하지 않지만 응답의 content 형식을 좌우하는 전이 의존성이라 함께 고정한다
langchain-core==1.4.7

# Database ORM
SQLAlchemy

# PDF 리포트 의존성은 없다.
#
# fpdf2(LGPL-3.0-only)로 서버에서 PDF 를 만드는 /generate-pdf 엔드포인트가 있었지만
# 프론트엔드가 한 번도 호출하지 않았다 — 화면의 "결과 리포트 저장"은 브라우저에서
# jsPDF + html2canvas(둘 다 MIT)로 만든다. 쓰지 않는 경로 하나 때문에 배포물에
# LGPL 고지 의무를 지고 있었으므로 엔드포인트와 함께 걷어냈다.
# 서버측 PDF 생성이 다시 필요해지면 fpdf2 를 되살리고 THIRD_PARTY_NOTICES.md 에
# LGPL-3.0 고지를 추가해야 한다.

# Environment Variables
python-dotenv==1.2.1

# HTTP Client for external APIs
# openrouter_client.call_openrouter (LangGraph 를 거치지 않는 직접 호출 경로)
httpx